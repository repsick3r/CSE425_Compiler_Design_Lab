import Random;

class Game {
  // Haxe applications have a static entry point called main
  static function main() {
	var Harish = Random.int(1A, 100);
	var sudhamsh  = Random.int(1, 100);
	if(sudhamsh > Hairhs) trace("Sudhamsh Wins!");
	else trace("Harish Wins!");
  }
}
