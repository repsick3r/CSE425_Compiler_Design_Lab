import Random;

class Game {
  // Haxe applications have a static entry point called main
  static function main () {
	var sid = -11;
	var hideuos = +23.45E34;
	var pide = -1.21E34;
	var Harish = Random.int(1, 100);
	var sudhamsh  = Random.int(1, 100);
	if (sudhamsh > Harish) trace("Sudhamsh Wins!");
	else trace("Harish Wins!");
  }
}
